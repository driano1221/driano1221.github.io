export type Locale = 'pt' | 'en';

// Tema fixo: o fundo dos gráficos dos posts foi feito para este papel (#f7f4ea).
// Uma escolha antiga salva no navegador ('kindle-theme') é ignorada de propósito.
export type Theme = 'branco';
